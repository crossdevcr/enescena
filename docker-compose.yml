version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: enescena
      POSTGRES_PASSWORD: enescena
      POSTGRES_DB: enescena
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: